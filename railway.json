{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --omit=dev --no-audit --prefer-offline"
  },
  "deploy": {
    "startCommand": "node index.js"
  }
}
